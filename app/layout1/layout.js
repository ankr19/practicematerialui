import Layout1 from "@/components/Layout/Layout1";
import React from "react";

export const metadata = {
  title: "Layout1 | For Design",
};

export default function Layout({ children }) {
  return <Layout1>{children}</Layout1>;
}
